name: flush
version: 1.0.0+1

flutter:
  uses-material-design: true

dependencies:
  flutter:
    sdk: flutter
  # Local Storages
  shared_preferences: ^0.5.3+4
  path_provider: ^1.2.0
  # Statements
  fluro: ^1.5.1
  provider: ^3.1.0
  # Network
  dio: ^2.1.16
  # Components
  flutter_icons: ^0.1.4
  flutter_slidable: ^0.5.3
  # Locales
  # intl: ^0.15.8
  flutter_localizations:
    sdk: flutter
  # QRCode Generate
  # qr_flutter: ^2.1.0+55
  # QRCode Scanner
  # qr_utils: ^0.1.4 # works fine but cannot custom ui, no close button
  # barcode_scan: ^1.0.0 # Cannot open camera on emulator
  # qrcode_reader: ^0.4.4 # Not working
  # fast_qr_reader_view: # Dependent on Firebase (blocked)
  #   git:
  #     url: git://github.com/facundomedica/fast_qr_reader_view.git
  qr_code_scanner: ^0.0.12

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.1.0 <3.0.0"
